---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('fi-FI', {
			year: '2-digit',
			month: '2-digit',
			day: '2-digit',
		})
	}
</time>
